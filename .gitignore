# Runtime ledger must live in the target project, not this config repo.
# Ignore all of .agents/ EXCEPT .agents/plans/, which holds committed
# migration plans and retros for traceability.
.agents/*
!.agents/plans/
.DS_Store
__pycache__/
